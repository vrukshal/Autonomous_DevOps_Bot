# Production / Railway: full URL to API including /api (set at build time)
# VITE_API_BASE_URL=https://your-backend.up.railway.app/api

# Local dev: leave unset — Vite proxy uses /api → localhost:8000
