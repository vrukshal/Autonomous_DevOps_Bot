# Public Assets

Place your logo image file here as `logo.png`.

The logo component will automatically use `/logo.png` from the public directory.

If the logo file is not found, a fallback robot emoji will be displayed.
